# Where the project enquiry form POSTs its JSON payload.
# Works with Formspree, Web3Forms, Basin, or your own API route.
# If left unset, the form falls back to opening the visitor's mail client.
VITE_CONTACT_ENDPOINT=

# Optional separate endpoint for the footer newsletter signup.
VITE_NEWSLETTER_ENDPOINT=
